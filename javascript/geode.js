/*:
	@require:
		{
	
		}
	@end-require
*/

exports.generatePreAccessCode = function generatePreAccessCode( ){

};

/*
	geoLocation : {
		"latitude": 
		"longitude":
	}

	accessCode: {
		"MACaddress":
		"preAccessCode":
		"UID":
	}

	We will have access to the redis data through when we are using the pre-access code.
	The pre-access code is always stored in the redis and if confirmed it will stored
		to a database.
*/
exports.generateGeoLocationCode = function generateGeoLocationCode( geoLocation, accessCode ){

};



exports.boot = function boot( ){

};